name: ci
on: [pull_request]
jobs:
  Test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2
      - name: Setup scala
        uses: olafurpg/setup-scala@v10
      - name: Run tests
        run: "sbt test"
  
  Build:
    needs: Test
    runs-on: ubuntu-latest
    steps:
    - name: Checkout
      uses: actions/checkout@v2
    - name: Setup scala
      uses: olafurpg/setup-scala@v10
      
      
